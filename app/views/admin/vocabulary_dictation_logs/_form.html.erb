<%= form_with(model: admin_vocabulary_dictation_log) do |form| %>
  <% if admin_vocabulary_dictation_log.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(admin_vocabulary_dictation_log.errors.count, "error") %> prohibited this admin_vocabulary_dictation_log from being saved:</h2>

      <ul>
        <% admin_vocabulary_dictation_log.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
